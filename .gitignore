# Secrets — never commit API keys
.env
.env.local

# Milestone 0 verification workspace (synthetic smoke-test footage + outputs)
# Removed once the milestone is complete; real projects live under ~/Videos/vsf-projects/
_m0/

# Python
__pycache__/
*.py[cod]
*.egg-info/
.venv/
venv/
.pytest_cache/
.mypy_cache/
.ruff_cache/
build/
dist/
